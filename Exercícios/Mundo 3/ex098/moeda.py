# Estudo de Modularização
def aumentar(preco, taxa):
    res = preco + (preco * taxa)/100
    return res


def diminuir(preco, taxa):
    res = preco - (preco * taxa)/100
    return res
    

def dobro(preco):

def metade(preco):
